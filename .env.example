# Copy to .env (gitignored) and fill in. NEVER commit real values.

# Company data — read-only Postgres (world schema). Get the live string from the
# Atlan AI Hackathon kickoff deck / #atlan-ai-hackathon-2026. Do NOT paste it here.
WORLD_PG_URI=

# Your PARTICIPANT Anthropic workspace key (from 1Password). Never the judge key.
ANTHROPIC_API_KEY=
